import 'package:flutter/material.dart';

class MyTheme {
  static const Color lightPrimary= Color(0xFF000A32);
  static final ThemeData lightTheme = ThemeData(
    primaryColor: lightPrimary,
      appBarTheme: const AppBarTheme(
        color: lightPrimary

      )

  );

}

